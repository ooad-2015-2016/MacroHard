<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ShEM.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModel="using:ShEM.ViewModel"
    x:Class="ShEM.View.BlankPage4"
    mc:Ignorable="d">
    <Page.Resources>
        <DataTemplate x:Key="UserTemplate">
            <Grid Height="110" Width="480" Margin="10" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Background="{StaticResource ListViewItemPlaceholderBackgroundThemeBrush}" Width="110" Height="110">
                    <Image Height="110" Width="110"/>
                </Border>
                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock Text="{Binding email}" Style="{StaticResource TitleTextBlockStyle}"/>
                    <TextBlock Text="{Binding password}" Style="{StaticResource CaptionTextBlockStyle}" TextWrapping="NoWrap"/>
                </StackPanel>
            </Grid>
        </DataTemplate>
        <Color x:Key="Color1">#FFD32F2E</Color>
    </Page.Resources>

    <Page.DataContext>
        <ViewModel:NewsFeedViewModel/>
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" DataContext="{Binding}">
        <Rectangle HorizontalAlignment="Left" Height="257" Margin="0,10,0,0" Stroke="Black" VerticalAlignment="Top" Width="1920" Fill="#D32F2E"/>
        <ListView x:Name="listView" HorizontalAlignment="Left" Height="682" Margin="95,330,0,0" VerticalAlignment="Top" Width="1720" FontFamily="Verdana" BorderThickness="2" RequestedTheme="Dark" ItemTemplate="{StaticResource UserTemplate}" ItemsSource="{Binding users}">
            <ListView.BorderBrush>
                <SolidColorBrush Color="{StaticResource Color1}"/>
            </ListView.BorderBrush>
        </ListView>
        <TextBlock x:Name="textBlock" HorizontalAlignment="Left" Margin="746,91,0,0" TextWrapping="Wrap" Text="News feed" VerticalAlignment="Top" FontFamily="Verdana" FontSize="75" Foreground="White"/>
        <Button x:Name="logoutButton" Content="Logout" HorizontalAlignment="Left" Margin="10,99,0,0" VerticalAlignment="Top" Width="375" Height="83" FontFamily="Verdana" FontSize="35" Background="#D32F2E" Foreground="White"/>

    </Grid>
</Page>
